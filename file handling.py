# Files: store the data 
# to read,write the data used the file for large dataset
# to sotre the permanent data and retrieve the data
# variables only store the temporary data.

# opening file :used open() function
# file_object=open(file_name,mode,encoding)
#  "file_name": the name or path of the file
# mode: to read,  write 
# encoding: utf-8 

# open,close,with
# read,character,readline,readlines(list format m answer show hoga),strip()(only in readline but it use in readlines to use loop),line count
# character and words count(split,splitlines)
